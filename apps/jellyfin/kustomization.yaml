apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jellyfin

# Jellyfin runs on the NAS now (see README). Only routing + EPG cronjob remain here;
# the Deployment + its PVCs/PV were removed (compute moved off-cluster).
resources:
  - service.yaml
  - service-lb.yaml
  - ingress.yaml
  - serbian-epg-cronjob.yaml
