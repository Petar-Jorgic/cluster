apiVersion: apps/v1
kind: Deployment
metadata:
  name: snacks
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: snacks
    app.kubernetes.io/part-of: snacks
spec:
  # Deaktiviert (nicht gelöscht): kein Platzgewinn bei DeleteOriginalFile=false,
  # CPU-Last/Strom auf der Control-Plane, kollidiert mit Hardlink/Seeding.
  # Reaktivieren: replicas auf 1.
  replicas: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/managed-by: kustomize
      app.kubernetes.io/name: snacks
      app.kubernetes.io/part-of: snacks
  template:
    metadata:
      labels:
        app.kubernetes.io/managed-by: kustomize
        app.kubernetes.io/name: snacks
        app.kubernetes.io/part-of: snacks
    spec:
      nodeSelector:
        kubernetes.io/hostname: pj
      securityContext:
        supplementalGroups:
          - 44
          - 109
          - 992
      containers:
        - name: snacks
          image: derekshreds/snacks-docker:2.5.1
          env:
            - name: TZ
              value: "Europe/Berlin"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: SNACKS_WORK_DIR
              value: "/app/work"
            # pj hat eine AMD-GPU (Renoir) -> VAAPI braucht radeonsi, NICHT den
            # Default iHD (Intel). Sonst scheitert vaInitialize und Jobs hängen auf "processing".
            - name: LIBVA_DRIVER_NAME
              value: "radeonsi"
          ports:
            - name: http
              containerPort: 6767
          securityContext:
            privileged: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 4000m
              memory: 2Gi
          volumeMounts:
            - name: media
              mountPath: /app/work/uploads
            - name: config
              mountPath: /app/work/config
            - name: logs
              mountPath: /app/work/logs
            - name: dri
              mountPath: /dev/dri
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: snacks-media
        - name: config
          persistentVolumeClaim:
            claimName: snacks-config
        - name: logs
          emptyDir: {}
        - name: dri
          hostPath:
            path: /dev/dri
