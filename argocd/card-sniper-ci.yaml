apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: card-sniper-ci
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Petar-Jorgic/cluster.git
    targetRevision: HEAD
    path: apps/card-sniper-ci
  destination:
    server: https://kubernetes.default.svc
    namespace: card-sniper-ci
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
