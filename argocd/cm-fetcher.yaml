apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cm-fetcher
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Petar-Jorgic/cluster.git
    targetRevision: HEAD
    path: apps/cm-fetcher
  destination:
    server: https://kubernetes.default.svc
    namespace: cm-fetcher
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
