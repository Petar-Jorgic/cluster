apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reisenotiz
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Petar-Jorgic/cluster.git
    targetRevision: HEAD
    path: apps/reisenotiz
  destination:
    server: https://kubernetes.default.svc
    namespace: reisenotiz
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
